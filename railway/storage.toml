[build]
  builder        = "DOCKERFILE"
  dockerfilePath = "Dockerfile"

  [build.dockerBuildArgs]
    SERVICE = "storage"

[deploy]
  startCommand            = "node dist/apps/storage/main.js"
  restartPolicyType       = "ON_FAILURE"
  restartPolicyMaxRetries = 5
